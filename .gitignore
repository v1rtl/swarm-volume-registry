# Foundry build artifacts
contracts/out/
contracts/cache/

# Broadcast logs — keep production deploy records, drop local/dry-run noise
contracts/broadcast/**/dry-run/
contracts/broadcast/*/31337/

# Bun workspace (packages/*, workers/*). bun.lock is committed.
node_modules/
packages/*/dist/
workers/*/.wrangler/
workers/*/.dev.vars

# Environment
.env
.env.*

# Editor / OS
.DS_Store
*.swp
